name: WhitelistDmc
version: '${project.version}'
main: main.WhitelistDmc
api-version: 1.15
author: Izocel
commands:
   w-link:
      description: Used to authenticate your Discord and Minecraft accounts.
      usage: /<w-link-command>
   w-hyperlinks:
      description: Used to send dynamic hyperlinks.
      usage: /<w-hyperlinks-command>
   testcmd:
      description: Used to test in 'dev' mode
      usage: /testcmd
